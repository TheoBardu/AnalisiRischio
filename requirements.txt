# Interfaccia
PyQt5>=5.15
PyQtWebEngine>=5.15

# Solo macOS e solo estetica: serve a rendere trasparente la barra del titolo
# e a far salire il contenuto fin sotto di essa. Senza, l'applicazione funziona
# lo stesso e macOS disegna la sua barra del titolo sopra la pagina.
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'

# Lettura e scrittura dei fogli di calcolo.
# pandas <3 e' un vincolo del backend: analisi_datiVR.py converte le colonne
# data con assegnazioni posizionali (df.iloc[:, N] = ...), che pandas 3 non
# accetta piu' perche' le colonne di testo hanno dtype 'str' e la scrittura
# posizionale non puo' cambiarne il tipo.
pandas>=2.0,<3
openpyxl>=3.1
numpy>=1.24

# Relazione Word (serviranno quando verra' implementato relazione/generatore.py)
python-docx>=1.1
docxtpl>=0.17

# Non installabili con pip:
#   LibreOffice  - comando 'soffice' nel PATH, solo per l'export dei PDF
#   VRR_analisiDati e VRV_analisiDati - cartelle sorelle di questo progetto,
#   indicate in ~/.analisirischio/config.json
